// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {FarmingPool} from "../src/farming__FarmingPool.flat.sol";
import {IERC20} from "../src/farming__FarmingPool.flat.sol";
import {IERC20Metadata} from "../src/farming__FarmingPool.flat.sol";

// [approx] mock for interface IERC20: all methods return fixed defaults; branches on its return values are not reproduced.
contract ESBMCMock_IERC20 is IERC20 {
  function totalSupply() external pure override returns (uint256) { return 0; }
  function balanceOf(address) external pure override returns (uint256) { return 0; }
  function transfer(address, uint256) external pure override returns (bool) { return false; }
  function approve(address, uint256) external pure override returns (bool) { return false; }
  function transferFrom(address, address, uint256) external pure override returns (bool) { return false; }
  function allowance(address, address) external pure override returns (uint256) { return 0; }
}

// [approx] mock for interface IERC20Metadata: all methods return fixed defaults; branches on its return values are not reproduced.
contract ESBMCMock_IERC20Metadata is IERC20Metadata {
  function transferFrom(address, address, uint256) external pure override returns (bool) { return false; }
  function approve(address, uint256) external pure override returns (bool) { return false; }
  function transfer(address, uint256) external pure override returns (bool) { return false; }
  function decimals() external pure override returns (uint8) { return 0; }
  function symbol() external pure override returns (string memory) { return ""; }
  function name() external pure override returns (string memory) { return ""; }
  function balanceOf(address) external pure override returns (uint256) { return 0; }
  function totalSupply() external pure override returns (uint256) { return 0; }
  function allowance(address, address) external pure override returns (uint256) { return 0; }
}

contract FarmingPoolCovTest_FarmingPool_rescueFunds is Test {
  ESBMCMock_IERC20Metadata mk_IERC20Metadata_stakingToken_;
  ESBMCMock_IERC20 mk_IERC20_rewardsToken_;
  ESBMCMock_IERC20 mk_IERC20_token;
  FarmingPool c0;
  function setUp() public {
    mk_IERC20Metadata_stakingToken_ = new ESBMCMock_IERC20Metadata();
    mk_IERC20_rewardsToken_ = new ESBMCMock_IERC20();
    mk_IERC20_token = new ESBMCMock_IERC20();
    c0 = new FarmingPool(mk_IERC20Metadata_stakingToken_, mk_IERC20_rewardsToken_, address(uint160(1)));
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:127, sol:@C@FarmingPool@F@rescueFunds#6774:path:126
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_0() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 0);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:127, sol:@C@FarmingPool@F@rescueFunds#6774:path:126
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_1() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 1);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:251, sol:@C@FarmingPool@F@rescueFunds#6774:path:250, sol:@C@FarmingPool@F@rescueFunds#6774:path:123, sol:@C@FarmingPool@F@rescueFunds#6774:path:122, sol:@C@FarmingPool@F@rescueFunds#6774:path:60
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_2() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 1) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 0);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:124
  function test_cov_3() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 115792089237316195423570985008687907853269984665640564039457584007913129639935) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:127, sol:@C@FarmingPool@F@rescueFunds#6774:path:126
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_4() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 1);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:251, sol:@C@FarmingPool@F@rescueFunds#6774:path:250, sol:@C@FarmingPool@F@rescueFunds#6774:path:123, sol:@C@FarmingPool@F@rescueFunds#6774:path:122, sol:@C@FarmingPool@F@rescueFunds#6774:path:60
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_5() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 1) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 0);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:124
  function test_cov_6() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 115792089237316195423570985008687907853269984665640564039457584007913129639935) {} catch {}
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 0) {} catch {}
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:127, sol:@C@FarmingPool@F@rescueFunds#6774:path:126, sol:@C@FarmingPool@F@rescueFunds#6774:path:2
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_7() public {
    vm.prank(address(uint160(0)));
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 1);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:251, sol:@C@FarmingPool@F@rescueFunds#6774:path:250, sol:@C@FarmingPool@F@rescueFunds#6774:path:124, sol:@C@FarmingPool@F@rescueFunds#6774:path:123, sol:@C@FarmingPool@F@rescueFunds#6774:path:122, sol:@C@FarmingPool@F@rescueFunds#6774:path:60
  // DISABLED: RED on the unmodified contract, so its
  // coverage is not ours to claim. Kept, renamed out of
  // forge's `test*` prefix, so the artefact still shows
  // what was generated.
  function disabled_test_cov_8() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 1) {} catch {}
    // [asserted] path exits normally; a revert fails the test
    c0.rescueFunds(mk_IERC20_token, 0);
  }
  // claim: sol:@C@FarmingPool@F@rescueFunds#6774:path:6
  function test_cov_9() public {
    vm.prank(address(uint160(1)));
    // [revert-tolerant] outcome not asserted
    try c0.rescueFunds(mk_IERC20_token, 1) {} catch {}
  }
}
