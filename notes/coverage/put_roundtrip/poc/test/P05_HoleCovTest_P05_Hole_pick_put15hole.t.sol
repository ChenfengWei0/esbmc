// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {P05_Hole} from "../src/P05_Hole.sol";

contract P05_HoleCovTest_P05_Hole_pick_put15hole is Test {
  P05_Hole c0;
  function setUp() public {
    c0 = new P05_Hole();
  }
  // claim: sol:@C@P05_Hole@F@pick#30:path:15
  function test_cov_0() public {
    // [asserted] path exits normally; a revert fails the test
    c0.pick(100);
  }
  // claim: sol:@C@P05_Hole@F@pick#30:path:14
  function test_cov_1() public {
    // [asserted] path exits normally; a revert fails the test
    c0.pick(115792089237316195423570985008687907853269984665640564039457584007913129639935);
  }
  // claim: sol:@C@P05_Hole@F@pick#30:path:6
  function test_cov_2() public {
    // [revert-tolerant] outcome not asserted
    try c0.pick(42) {} catch {}
  }
  // claim: sol:@C@P05_Hole@F@pick#30:path:2
  function test_cov_3() public {
    vm.deal(address(this), 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // [asserted] value sent to a NON-PAYABLE entry: the call must fail
    (bool ok4, ) = address(c0).call{value: 115792089237316195423570985008687907853269984665640564039457584007913129639935}(
        abi.encodeWithSignature("pick(uint256)", 0));
    assertFalse(ok4, "value sent to a non-payable entry must revert");
  }

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@P05_Hole@F@pick#30:path:15   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   x in [0, 100]  \ {42}
  //   PIN msg.value == 0
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: x
  // ORACLE: 3 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 3 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  function test_put_P05_Hole_pick_path15(uint256 x) public {
    x = bound(x, 0, 100);
    vm.assume(x != 42);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_seen = uint256(vm.load(address(c0), bytes32(uint256(0))));
    // [asserted] path exits normally; a revert fails the test
    c0.pick(x);
    uint256 _post_seen = uint256(vm.load(address(c0), bytes32(uint256(0))));
    assertTrue(_post_seen != _pre_seen, "seen: post != pre");
    assertGe(_post_seen, _pre_seen, "seen: post >= pre");
    assertGt(_post_seen, _pre_seen, "seen: post > pre");
  }
}
