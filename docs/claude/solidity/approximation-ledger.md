# Approximation Ledger (soundness & completeness trade-offs)

**Purpose.** This section records every deliberate abstraction in the
Solidity frontend that sacrifices soundness or completeness. Each entry
documents (1) where the approximation lives, (2) whether it is
over-approximate or under-approximate, (3) what kinds of false positives
or false negatives it can produce. Code sites carry an `[APPROX: OVER]`
or `[APPROX: UNDER]` marker that mirrors the entries here — grep for
`\[APPROX:` to find every in-source warning.

## Terminology

- **Over-approximation (sound, incomplete)**: the model admits *more*
  behaviours than the real system. Counterexamples may be spurious
  (false positives). Proofs of safety carry over to the real system.
- **Under-approximation (unsound, may be complete)**: the model admits
  *fewer* behaviours. Bugs reachable only in the missing behaviours are
  not detected (false negatives). Counterexamples are real bugs.

"Sound for safety" = no real bug is missed. "Sound for equality" = the
abstraction is deterministic and injective; reasoning about identity
holds but reasoning about bit patterns does not.

## Ledger

| # | Area | Site | Direction | Rationale | False positives | False negatives |
|---|------|------|-----------|-----------|-----------------|-----------------|
| 1 | Inline assembly | `solidity_convert_stmt.cpp::InlineAssemblyStatement` + `try_lower_yul_block_precise` | partial-precise + OVER fallback | T2.4 (2026-04-30; `break`/`continue` added 2026-05-02; `div`/`mod` if-guard dropped 2026-05-02 to expose div-by-zero check; `sload(X.slot)`/`sstore(X.slot, v)` on scalar state vars added 2026-05-03) — the Yul body is translated precisely whenever the entire block lives in the supported subset (`let` / `:=` / `add sub mul div mod addmod mulmod` / `lt gt slt sgt eq iszero` / `and or xor not` / `shl shr` / `if` / `switch` / `for` / `break` / `continue` / `sload(X.slot)` and `sstore(X.slot, v)` where X is a scalar state variable (uint*/int*/bool/address) / nested `YulBlock` / number+bool literals). Any block containing an unsupported Yul construct (memory/calldata ops, hashing, calls, returns/reverts, EVM intrinsics, Yul function defs, `leave`, `.offset`, computed-slot or non-scalar-state-var sload/sstore, multi-LHS assignment, hex/string literals) falls back to havocing every `externalReferences` declaration (including `.slot`-referenced state vars) AND emits a `[approx]` warning naming the offending construct + source location. **Yul `div`/`mod`/`addmod`/`mulmod`** lower directly to `div_exprt`/`mod_exprt` (no if-guard) so goto_check's `--div-by-zero-check` (on by default) fires on reachable zero divisors and reports "division by zero". The prior if-guard `if(b==0) 0 else bvudiv(a,b)` silenced this check entirely (the false-branch's path guard `b != 0` made the assert tautological). **`sload(X.slot)`** lowers to a typecast read of state var X widened to uint256; **`sstore(X.slot, v)`** lowers to `code_assignt(X, narrow_to_native(v))` — same SSA path as a Solidity-level `X = v`. Slot resolution uses solc's `externalReferences[].declaration` ID; we never compute slot numbers numerically, so computed slots (`sload(0)`, `sload(add(X.slot, 1))`, `sload(keccak256(...))`) and non-scalar state vars (struct/mapping/array/bytes/contract) trip the all-or-nothing rule and fall back to havoc. The all-or-nothing rule (whole block precise OR whole block havoc'd) keeps the precise portion sound. | Assembly-enforced invariants on havoc-fallback variables cannot be verified (unchanged). For the precise subset, no false positives. | (a) For the havoc fallback, writes the assembly *would have made but we skipped* are reflected as havoc, so no bug is hidden. (b) **`--no-div-by-zero-check` soundness gap**: with the check disabled, Yul `div(_, 0)` evaluates to SMT-LIB's bvudiv-zero result (all-1s = MAX), NOT Yul's spec value 0. Default checks ON closes this; opting out is the user's responsibility. (c) Computed-slot and non-scalar-state-var sload/sstore still over-approximate via havoc — assembly-only writes via `keccak256`-derived mapping slots are not reflected back into the Solidity-level mapping value (the legacy fallback havocs the mapping symbol but not its individual entries). |
| 2 | Crypto hashes | `solidity_crypto.c` (keccak256/sha256/ripemd160/ecrecover) | OVER + UNDER | Identity-like bijective abstraction (`~x`, `~(x+1)`, …). Deterministic, injective, distinct families. | Properties of the form `keccak256(0) == 0xc5d2...` (specific hash bits) cannot be proved. | Preimage / collision / signature-forgery properties cannot be refuted; `ecrecover` ignores `(v,r,s)`. |
| 3 | ABI encode | `solidity_abi.c` (`abi_encode*`, `abi_encodeCall`, `abi_encodeWithSelector`, `abi_encodeWithSignature`, `abi_encodePacked`) + `solidity_convert_expr.cpp` (multi-arg fold) | OVER + UNDER | **CLOSED 2026-04-30 (F1).** The unsound multiplicative `_ESBMC_abi_fold` was deleted. The frontend now captures each arg's pre-cast bit-width, concats args (`arg[0]` high, `arg[N-1]` low) into a wide BV, picks bucket `W ∈ {256, 512, 1024, 2048}` covering total arg-bits, and routes through bucketed `_ESBMC_<family>_table_<W>` arrays declared `__attribute__((annotate("__ESBMC_inf_size:<W>")))` — same wide-BV infrastructure as F2/F3 (commits 9bd1cecf92 + 66874969b4). At each fold-path call site the frontend emits: `(1)` allocate static globals `__esbmc_hash_key_<family>_<W>_<n>` and `__esbmc_hash_result_<family>_<W>_<n>`; `(2)` table-memoised lookup with sentinel-0 — `existing = table[key]; result = (existing == 0) ? nondet() : existing; assume(result != 0); table[key] = result` — gives `same key → same hash` via the SMT array axiom (cross-call AND cross-site); `(3)` per-pair distinctness assume `prior_key == this_key || prior_result != this_result` against every prior matching call site, encoding the injectivity axiom that array theory does not provide. F1 KNOWNBUG `abi_fold_collision_distinct_pass_knownbug` flipped to CORE PASS; renamed dir drops the `_knownbug` suffix. Tests `abi_decode_2`, `abi_encodeCall_3`, `abi_encodeWithSignature_3`, `abi_encode_3`, `hash_4`, `keccak_abi_encode_returns_bytes32_fail` continue to FAIL correctly — the new model is strictly more sound; assertions that the legacy-fold collision had violated by ACCIDENT are now violated by INTENTION. | Packed byte-layout properties (selector presence, delimiters, ABI-spec padding) cannot be verified — fold collapses to a single uint256 hash, not bit-accurate. The `result != 0` sentinel carves out one specific hash value (≈ 2^-256 collision probability under real EVM keccak, negligible). | (a) Two `bytes` args of equal length but different content — fold is length-only, not offset-aware (deferred B7 follow-on). (b) `t_bytesN` (fixed bytes1..bytes32) args — BytesStatic struct still skipped (deferred). (c) `encodeCall(this.f, ())` skips its function-ref arg → fresh nondet fallback per call. (d) Total arg-width > 2048 falls back to nondet result with `[approx]` warning — extending the bucket schedule is straightforward if a test hits it. (e) Cross-iteration distinctness for the same syntactic call site: globals are overwritten each iteration, so the per-pair assume between two iterations of the same site does not fire. F1 KNOWNBUG has no loop; closure unaffected. |
| 4 | ABI decode | `solidity_abi.c::abi_decode` | OVER | Returns nondet uint256_t. | None — every concrete value is admitted. | Round-trip `abi.decode(abi.encode(x)) == x` is NOT provable (decoder is detached from encoder). |
| 5 | `msg` / `tx` / `block` variables | `solidity_blockchain.c` + `solidity_misc.c::_sol_per_tx_reseed` | OVER | All fields backed by per-tx globals reseeded once per harness iteration (NOT on every access). Within a call: stable. Across calls: `msg.*` / `tx.*` fresh nondet, `block.number` / `block.timestamp` constrained monotonic non-decreasing, `tx.origin == msg.sender` invariant for the top-level call frame (T2.3, 2026-04-30). Other `block.*` (basefee, coinbase, difficulty, gaslimit, prevrandao) fully nondet per iteration. | (a) Only the top-level-call invariant is enforced; nested-call relationship between caller-contract address and its own `msg.sender` isn't modelled (harness drives top-level only). (b) `block.coinbase` / `block.difficulty` etc. without monotonicity. | None for safety. |
| 6 | `blockhash` / `blobhash` | `solidity_blockchain.c` | OVER | Nondet uint256_t. | Properties of specific block hashes. | None for safety. |
| 7 | Entry-harness unbound dispatch | `solidity_convert_constructor.cpp::get_unbound_function` | OVER | Each public/external method called inside a nondet-guarded branch with nondet arguments, nondet msg.sender/value. | Order-of-call invariants ("init before transfer") fail spuriously; payable assertions like `assert(msg.value > 0)` fire on entries that solc's original test exercised only with value > 0 (e.g. `stress_libsol_fntype_inline_array_value_call`). | Multi-transaction bugs whose reachability depends on state surviving across *transactions* are not explored unless `--multi-transaction` mode is used. |
| 8 | External call re-entry (unbound mode) | `solidity_convert_contract.cpp::get_unbound_expr` + `solidity_convert_expr.cpp` low-level call path | OVER | `addr.call` / `delegatecall` / `staticcall` return `(nondet bool, nondet bytes)`; side-effect re-enters the *current* contract's nondet dispatch; callee address is ignored. | Reentrancy paths are explored unconditionally even for callees that would revert, causing extra counterexamples for properties that assume specific callee behaviour. | Cross-contract effects on the actual callee address are invisible. |
| 9 | Bytes-init for harness params | `solidity_convert_call.cpp::assign_param_nondet` + `solidity_builtins.c::llc_nondet_bytes` | OVER (init only) | Harness-generated `bytes calldata` / `bytes memory` parameters flow through `llc_nondet_bytes()` which sets `initialized == 1` and `capacity == length`; `length` itself is fully nondet `size_t` (post-T1.2, 2026-04-30). Direct user declarations (`bytes memory x;`) keep precise init/bounds checks. | None for length range — every gas-bounded length is admissible. | None for length range. The single remaining over-approximation is `initialized == 1` so callees do not fire spurious init checks; real uninit-bytes bugs in user code still surface via the normal struct-init path. |
| 10 | Calldata array-of-bytes | `solidity_convert_expr.cpp::get_index_access_expr` (calldata `bytes[] / bytes[N]` element read) | UNDER | When the base array is calldata (`#sol_data_loc == "calldata"`) and the element type is `BYTES_DYN`, `a[i]` is replaced with a fresh `llc_nondet_bytes()` — independent per access. Post-T1.2: length is fully nondet (no [32, 1024] clamp). Storage / memory `bytes[] x;` keeps the precise index_exprt path. | None for length range. | Repeated reads of the same `a[i]` are **independent samples** (no `a[i] == a[i]` invariant). Workaround: copy into a local `bytes memory b = a[i];` to materialise once and read from `b`. `string[] calldata` elements still stay on the precise path because `string` lowers to `char*` and type-mismatches `BytesDynamic` — those tests remain `KNOWNBUG`. |
| 11 | Function-reference identity | `solidity_convert_expr.cpp::MemberAccess` (used-as-value) | OVER (for identity) + UNDER (for content) | `this.f` as a value lowers to `(void*)(fn_id + 1)` — stable, distinct per callee, so `this.f == this.f` and `this.f != this.g` hold. | None for identity comparisons. | Indirect calls through fn-ptrs never execute the real body; `solidity_convert_call.cpp` substitutes a nondet return of the declared type. Bugs inside functions reachable only via an indirect call are invisible. |
| 12 | Indirect callees without `referencedDeclaration` | `solidity_convert_expr.cpp::get_call_expr` | UNDER | Ternary on fn refs, `IndexAccess` on a fn-ptr mapping, etc. → call result is nondet of the declared return type; the real body is never invoked. | None (the nondet return covers every value). | Side effects and bugs in the target function are not observed. |
| 13 | Function-typed r-value arguments | `solidity_convert_call.cpp` (`t_function_internal_` / `t_function_external_` branch) | UNDER | Passing a fn ref as an argument substitutes an opaque nondet pointer. The callee will dispatch it via #12 above. | None. | See #12. |
| 14 | IndexRangeAccess slices (`b[s:e]`) | `solidity_convert_expr.cpp::get_index_range_access_expr` | OVER (content) — bounds closed | Slice value is still a fresh nondet detached from the parent's content. Bound assumes added 2026-04-30: `__ESBMC_assume(s <= e)` (always when both endpoints resolved) and `__ESBMC_assume(e <= base.length)` (when `base` is a `t_bytes_*` whose `.length` is reachable; T[] calldata defers). Real-EVM slice reverts when these are violated, so the model now matches that path-pruning. Locked by `slice_bounds_pass` + `slice_bounds_violation_fail`. | Same-content slice assertions still cannot be verified (slice is still detached nondet). | None for safety. |
| 15 | `type(I).interfaceId` / `type(C).creationCode` / `type(C).runtimeCode` | `solidity_misc.c::_interfaceId`, `_creationCode`, `_runtimeCode` + `solidity_convert_ref.cpp` (per-name id assignment) | identity-bijective | Stable per name (post-2026-05-01): the frontend assigns a unique uint32 id per `(property, contract-or-interface-name)` pair via `interface_id_table`, then the helper returns `~id` (bijective on uint32). Two reads of `type(I).interfaceId` yield the same value; distinct interfaces yield distinct values. Same family as the crypto-hash abstraction. Locked by `interfaceId_stable_pass`, `interfaceId_distinct_pass`. | Specific bit-pattern interface-id checks (`type(I).interfaceId == 0x01ffc9a7`) still cannot be proved — the value is the model's `~id`, not the real selector-XOR. | None for safety. |
| 16 | `revert` / `require` / failed `.transfer()` | `solidity_convert_modifier.cpp::build_revert_rollback_block` (B1) + `solidity_convert_stmt.cpp::TryStatement` if/else nondet split + `solidity_convert_call.cpp::get_transfer_definition` underflow guard | UNDER for state, OVER for control flow | Public/external reverts run B1's `*this = _sol_save_this; return;`. Internal/private reverts and transfer-failure paths still emit `__ESBMC_assume(false)`. Try/catch is modelled as `if (nondet) { call; success } else { catch }` — catch sits in the else branch so SSA branching keeps success-arm writes out of catch's view (T2.2 confirmed empirically). Cross-iteration in the dispatcher harness is also covered by SSA branching at the while-loop join + path pruning (B5-B confirmed empirically, 2026-04-30). Standalone `addr.transfer()` outside try/catch closes 4 of 5 sub-cases (2026-04-30): `transfer_standalone_invariant_pass`, `transfer_standalone_pre_revert_post_write_isolation_pass`, `transfer_standalone_cross_iter_pruned_pass`, `transfer_standalone_partial_state_unobservable_pass` are empirically locked CORE. The fix that closed `partial_state_unobservable` was the missing underflow guard `__ESBMC_assume(this->$balance >= _val)` on the EOA-transfer fall-through path (commit `49e1bcc4e6`). | None for try/catch state rollback or cross-iteration leak via internal-helper revert (closed by SSA branching + B1, regression-locked by `try_catch_*_rollback_pass` and `cross_iter_internal_revert_*_pass`). For standalone transfer, intermediate-state isolation + debit invariant closed (4 CORE PASS regressions). | (a) **Multi-instance same-type dispatch** — the address-match in `get_transfer_definition` (`solidity_convert_call.cpp:3472-3479`) only checks `_addr == _ESBMC_Object_<C>.$address`, the single static instance per contract type. When there are multiple instances of the same type (`a, b = new Vault()` twice), recipient transfers fall through to the EOA path, which credits the EOA balance map but NOT the recipient's `$balance`; subsequent `address(b).balance` reads observe the un-credited value. Closing this requires either a 160-bit address allocator with provable distinctness from literal address constants OR a dispatch redesign with collision-protection. KNOWNBUG `transfer_standalone_balance_invariant_pass` regression-locks the gap. (b) `addr.transfer()` revert outside try/catch in the SAME-FRAME setup: writes before transfer are still committed in the SSA on the pruned path, so observers downstream of a NESTED call frame can still see partial state if no try/catch wraps the call. |
| 17 | Uninitialized internal function pointers | (no explicit code) inline assembly read of internal fn-ptr tag | OVER (via inline assembly havoc) | Reading `z := t` in assembly havocs `z`; the real legacy-codegen panic tag / yul 0-init distinction is not modelled. | Tests that assert a specific tag value (`z != 0` on legacy, `z == 0` on yul) fail. | `stress_libsol_uninit_fnptr_*` cannot be fixed under a single model. |
| 18 | Multi-inheritance linearisation | `solidity_convert_inheritance.cpp` | sound-so-far | C3 linearisation follows solc; no known false positives or negatives. | — | — |
| 19 | Internal pool for `bytes` / `string` | `solidity_bytes.c::BytesPool` | OVER | Single monotonically-growing pool per contract instance; `free` is a no-op. | Pool-capacity exhaustion on very long runs is unreachable (pool is practically infinite in model). | None for bytes semantics. |
| 20 | `selfdestruct` | `solidity_builtins.c::selfdestruct` | UNDER | Modelled as `exit(0)` — terminates the harness path. Ether transfer and subsequent state reachability are not modelled. | None. | Post-selfdestruct behaviour of the destroyed contract (address reuse, CREATE2 re-deployment) is not explored. |
| 21 | Free-function `bytes memory` return of a string literal | `solidity_convert_stmt.cpp::ReturnStatement` (free-function path) | OVER | When a free function returns a string literal as `bytes memory`, the conversion needs a dynamic pool, but free functions have no containing contract. The return value is replaced with `llc_nondet_bytes()`. Contract-member functions keep the precise `bytes_dynamic_from_string` path. | None for length-range / existence checks. | The actual byte content of the returned literal is discarded — two successive calls return independent samples (no `f() == f()` invariant). `stress_func_ptr_longdata_1` escapes this by using `keccak256(a) == keccak256(b)` which is already nondet under the abstract keccak model. |
| 22 | Mapping-key XOR fold (256→64) | `solidity_convert_mapping.cpp::xor_fold_key_to_64bit` + `solidity_mapping.c::_ESBMC_map_idx` + `array_type2t::index_width` (commits `9bd1cecf92`, `<wide-bv-S4>`) | sound | **Closed 2026-05-01.** The wide-BV array index infrastructure shipped at `9bd1cecf92` (S1-S3) extended `array_type2t` with an optional `index_width` field threaded through migrate/equality/hash/SMT. Stage S4: per-mapping array_typets are declared with `#esbmc_index_width = "256"` (`solidity_convert_type.cpp` MappingTypeName branch); `_ESBMC_map_storage` is annotated `__ESBMC_inf_size:480` and `_ESBMC_map_idx[_fast]` use bit-vector concat instead of XOR-fold; `clang_c_adjust::adjust_index` honours the explicit index width annotation when casting array indices. Path-1 (frontend direct `index_exprt`) and path-2 (library `_ESBMC_map_idx`) both index without truncation. Locked by `mapping_xor_fold_collision_pass`, `mapping_idx_fold_collision_pass`. | None — distinct keys produce distinct slots. | None — the model accepts the soundness-tightening with no observed regressions. |
| 23 | Address allocator (loose default 16-slot cap) | `_ESBMC_get_unique_address` in `solidity_address.c:84-107`; `--solidity-precise` opts into the for-loop variant | UNDER | The default loose variant unrolls `__ESBMC_assume(tmp != sol_addr_array[i])` for `i = 0..15`. The 16th prior allocation slot (`sol_addr_array[16]`) is uncovered, so the 18th `new C()` allocation is unconstrained against the 17th's `$address`. Audit S1.2 attempted to flip the default to the precise for-loop variant but reverted because precise's `--unwind` coupling breaks tests with low `--unwind` values. KNOWNBUG-locked by `address_allocator_17_distinct_pass_knownbug`. | Distinct-address-required tests at high allocation counts: SMT picks colliding addresses past slot 16. | Same. |
| 24 | `new C()` `$balance` for non-payable ctors | `solidity_convert_builtin.cpp:131-157` | OVER | Real EVM mandates `$balance == 0` for `new C()` (no `{value: V}`, non-payable ctor). Current model uses `_ndt_uint` (fully nondet), admitting model-only states real EVM cannot reach. Audit S1.1 attempted to tighten to `gen_zero` but reverted because the harness has no "between-call external transfers in" simulation; many tests rely on the nondet over-approximation to express `require(address(this).balance >= V)` paths. KNOWNBUG-locked by `new_contract_initial_balance_zero_pass_knownbug`; the prerequisite (method-entry $balance refresh) is open. | Properties of the form "if I just deployed C, its balance is 0" cannot be proved. | None for safety; the over-approximation only adds reachable-in-model states real EVM can't reach. |

## How to use this ledger

- **Before claiming "verification successful"** in a security review,
  check which approximations the contract relies on. If the property
  depends on a column marked "False positives" → the proof is real.
  If it depends on a column marked "False negatives" → the proof is
  NOT sound; re-verify with a tighter model or manual reasoning.
- **Before filing a bug on spurious counterexamples**, check this
  ledger. A counterexample rooted in row 7 (nondet msg.value), row 5
  (non-monotonic block numbers) or row 16 (cross-iteration state) is
  expected behaviour, not a bug.
- **Adding a new approximation**: drop an `[APPROX: OVER|UNDER]` marker
  at the code site and append a row here with the same rationale wording.

## In-source markers

Every approximation above has a matching code comment; `rg '\[APPROX:'`
finds the canonical list. Table rows and code comments MUST be kept in
sync. If you remove an approximation, delete the marker *and* the row.
