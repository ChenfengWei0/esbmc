KNOWNBUG
contract.sol
--contract H --bound --no-standard-checks --unwind 4 --no-unwinding-assertions
^VERIFICATION SUCCESSFUL$
# T1.1 Stage S5: outer-level alias bug FIXED via shift-add injective
# fold.  c1 outer slot is provably disjoint from c2 outer slot.
# Remaining bug, out of T1.1 scope: new uint256 with symbolic size
# routes to _ESBMC_alloc_array_sym which uses malloc not calloc, so
# inner-row data is nondet-init.  c2.arr[0][0] reads nondet, can be
# != 0.  Real Solidity zero-initializes memory dyn-arrays.  Fix is
# to calloc-init the symbolic-size allocator.  T1.1 follow-up.
