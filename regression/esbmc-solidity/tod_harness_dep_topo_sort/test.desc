KNOWNBUG
contract.sol
--contract Base --tod-race-check=add,inc --k-induction --max-k-step 20 --k-step 3
^VERIFICATION SUCCESSFUL$
# T1.1 Stage S2: addr-keyed dyn-array element access combined with the
# loose 16-slot `_ESBMC_get_unique_address` model causes the k-induction
# inductive step to time out (was sub-second on heap-malloc, now > 90s
# on SMT-array + addr-fold).  Property still holds — passes in ~34s
# under `--solidity-precise`.  Will flip back to CORE once the precise
# variant becomes the default (T3.5 in the broader roadmap).
