CORE
contract.sol
--contract Twin --solidity-path-coverage --solidity-max-tx 1 --path-cov-assert spec.json
^--path-cov-assert: unit 'sol:@C@Twin@F@bump#32' -- established 0 relation-backed entry assignment\(s\), assumed 2 region bound\(s\) \(0 hole\(s\) punched\) at entry and emitted 11 candidate assertion\(s\), 3 of them over the unit's own RETURN VALUE and the rest over 2 state variable\(s\)
^WARNING: --path-cov-assert: 1 state variable\(s\) carry NO candidate and appear in NO row below: flag \[ordering/interval rungs\] \(it resolves to a BOOLEAN
The equality rungs \(post == pre / post != pre\) ARE emitted for it -- only the ordering, interval and delta rungs are not
^--path-cov-assert: region NON-VACUITY witnessed
^--path-cov-assert: flag: post == pre  REFUTED
^--path-cov-assert: flag: post != pre  HOLDS$
^--path-cov-assert: total: post == pre  REFUTED
^--path-cov-assert: total: post != pre  HOLDS$
^--path-cov-assert: total: post >= pre  HOLDS$
^--path-cov-assert: total: post <= pre  REFUTED
^--path-cov-assert: total: post > pre  HOLDS$
^--path-cov-assert: total: post < pre  REFUTED
^--path-cov-assert: return: a value IS returned on this path \(REFUTED == yes\)  REFUTED
^--path-cov-assert: return: return == 0  REFUTED
^--path-cov-assert: return: return != 0  HOLDS$
^--path-cov-assert: ladder summary -- 11 candidate\(s\): 5 HOLDS, 6 REFUTED, 0 no verdict \(solver unknown\), 0 no verdict \(never reached the solver\)
