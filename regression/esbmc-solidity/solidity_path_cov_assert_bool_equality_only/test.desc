CORE
contract.sol
--contract Twin --solidity-path-coverage --solidity-max-tx 1 --path-cov-assert spec.json
^--path-cov-assert: unit 'sol:@C@Twin@F@bump#32' -- assumed 2 region bound\(s\) \(0 hole\(s\) punched\) at entry and emitted 8 candidate assertion\(s\) over 2 state variable\(s\)
^WARNING: --path-cov-assert: 1 state variable\(s\) carry NO candidate and appear in NO row below: flag \[ordering/interval rungs\] \(it resolves to a BOOLEAN
The equality rungs \(post == pre / post != pre\) ARE emitted for it -- only the ordering, interval and delta rungs are not
^--path-cov-assert: region NON-VACUITY witnessed
^--path-cov-assert: flag: post == pre  REFUTED
^--path-cov-assert: flag: post != pre  HOLDS$
^--path-cov-assert: total: post == pre  REFUTED
^--path-cov-assert: total: post != pre  HOLDS$
^--path-cov-assert: total: post >= pre  HOLDS$
^--path-cov-assert: total: post <= pre  REFUTED
^--path-cov-assert: total: post > pre  HOLDS$
^--path-cov-assert: total: post < pre  REFUTED
^--path-cov-assert: ladder summary -- 8 candidate\(s\): 4 HOLDS, 4 REFUTED, 0 no verdict \(solver unknown\), 0 no verdict \(never reached the solver\)
