# foundry_t20_forge_assert_bytes_fail

Negative dual: two content-different dynamic-bytes values → FAILED, confirming
the bytes comparison is genuinely content-sensitive (not vacuous). Matches real
forge (FAIL).
