# Raw Sourcify responses — regenerable via scripts/fetch_sources.py
etherscan_meta/

# Pre-upgrade originals — kept only for local diff during dev
sources/*/contract.sol.orig

# ESBMC-generated AST and harness artifacts — regenerable
sources/*/contract.solast
sources/*/tod_race_*_harness.sol
sources/*/tod_balance_*_harness.sol
